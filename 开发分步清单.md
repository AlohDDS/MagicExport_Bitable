# 飞书记录视图打印插件 · 开发分步清单（方案 v1）

> 范围：纯方案与执行清单，不含插件源码实现。
> 交付方式（已锁定）：飞书「自定义插件（侧边栏，填服务地址）」，开发期 localhost 预览；不使用「记录视图扩展 / 数据表视图」能力，不走 opdev 上传。
> 决策状态：**已启动开发**（用户于 2026-08-30 授权「启动开发」）。当前推进 P0/P0.5 本地可运行部分。

## 0.1 当前进度（2026-08-30）
- ✅ 已建立 Vite + Vue3 + TS 工程骨架（package.json / vite.config / tsconfig / index.html / src）。
- ✅ 已集成 vue-print-designer：main.ts 引入 Web Component 与样式；App.vue 渲染 `<print-designer>` 并封装工具栏（从飞书读取 / 载入示例 / 保存·加载模板 / 打印 / 导出 PDF / 主题切换）。
- ✅ 已写好飞书接入层 `src/feishu/sdk.ts`：`isInFeishu()`、`getTableInfo()`、`getSelectionVariables()`（单条→标量、多条→数组），含字段值 normalize。
- ✅ 模板持久化先行以 localStorage 实现（`src/data/templateRepo.ts`，P2 再切多维表格字段）。
- ✅ 依赖已装好（node_modules 76M）。踩坑记录：① registry.npmjs.org 在你这网络每请求 ~96s 不可用，已切 `registry.npmmirror.com`（淘宝源，秒级）；② 飞书 js-sdk 在镜像源缺失，已在 `.npmrc` 配 `@lark-base-open:registry=https://registry.npmjs.org` 单独走默认源；③ npm 默认缓存目录 `AppData\Local\npm-cache` 被系统锁写（EPERM），已用项目内 `.npmcache` 绕过；④ `@lark-base-open/js-sdk` 真实最新版是 `1.0.2`（曾误写 `^1.0.22` 导致 ETARGET）。
- ✅ 已对照 `node_modules/@lark-base-open/js-sdk/dist/index.d.ts` 与 `vue-print-designer/dist/web-component.d.ts` 校准 API：`getActiveTable/getSelection/getFieldList/field.getName()/field.getCellString(recordId)`、`setVariables/loadTemplateData/getTemplateData/print/export/setBranding/setTheme` 全部对得上。
- ✅ dev server 已启动：`http://localhost:5173/`（VITE v5.4.21 ready），所有模块与 vue-print-designer 依赖均 HTTP 200、无转译报错。
- ⏳ P0.5 飞书侧「建应用 + 自定义插件填 localhost」需用户用自己的账号操作，我提供分步说明。
> 技术栈：Vue3 + Vite + TS + vue-print-designer（Web Component）+ @lark-base-open/js-sdk。
> 许可：vue-print-designer 为 AGPL-3.0-only，内部小团队不改源码直接使用免费、无需授权。

## 0. 前期物料核对（开始前确认齐）
- [ ] 飞书开放平台账号 + 企业自建应用（已添加「多维表格插件 → 侧边栏插件」能力）
- [ ] 目标多维表格 读 / 写 权限
- [ ] Node ≥ 18、npm 或 pnpm 已安装
- [ ] 目标表字段结构清单（字段名 / 类型 / 示例值）
- [ ] 典型单据样例（出库单 / 标签 / 快递单）+ 纸张尺寸（A4 / 80mm 标签 / 热敏纸）
- [ ] 明确：单条记录打印 还是 选中多条批量打印
- [ ] 是否需静默打印（决定要不要 PrintDot Client）

## 1. P0 工程脚手架 + 引入 vue-print-designer
1. 初始化 Vite + Vue3 + TS 工程。
2. 安装依赖：vue-print-designer（peer：Vue3；Element Plus 由包内置，确认即可）。
3. 入口 main 引入 `vue-print-designer` 与其样式；页面放置 `<print-designer>` 自定义元素。
4. 本地 `npm run dev`，浏览器开 `localhost:5173` 确认空白设计器可加载。
✅ 里程碑：本地能看到 vue-print-designer 设计器界面。

## 2. P0.5 飞书「自定义插件（侧边栏）」接入 + localhost 预览
> 注：此处用的是**侧边栏自定义插件（填服务地址）**，不是「记录视图扩展 / 数据表视图」能力。
1. 飞书开放平台：企业自建应用 → 添加能力「多维表格插件 → 侧边栏插件」。
2. 多维表格内：插件面板 → 自定义插件 → +新增 → 填服务地址 `http://localhost:5173`（开发期）。
3. 安装 `@lark-base-open/js-sdk`。
4. 在插件里调用 `bitable.bridge.getUserId()` 验证已在飞书侧边栏环境运行。
✅ 里程碑：飞书侧边栏里能加载出本地设计器（热更新生效）。

## 3. P1 读取记录 + 变量映射
1. `bitable.base.getActiveTable()` 取当前表；`getSelection()` 取选中记录。
2. 读字段元数据（名 / 类型）+ 选中记录 cell 值。
3. 将记录字段拍平为变量对象（如 orderNo、customerName）；多行子表映射为数组变量。
4. `designer.setVariables(变量对象, { merge: true })` 注入。
5. 处理字段类型边界：附件 / 人员 / 公式等非常规字段的映射规则。
✅ 里程碑：选中一条记录，设计器里变量被真实数据填充。

## 4. P2 模板持久化（先存多维表格字段）
1. 在多维表格建一张「打印模板」表：字段含 模板名、模板 JSON（长文本）、纸张尺寸等。
2. 保存：`designer.getTemplateData()` 取模板 JSON → 写入「打印模板」表。
3. 加载：从表读模板 JSON → `designer.loadTemplateData(json)`。
4. 预留 Repository 接口，便于后期切 Supabase。
✅ 里程碑：设计好的模板能保存并在下次打开恢复。

## 5. P3 打印 / 导出闭环
1. 浏览器打印：`designer.print({ mode: 'browser' })`。
2. 导出 PDF：`designer.export({ type: 'pdf' | 'pdfBlob' })`（Blob 可回传存档）。
3. 出单预览界面（打印前确认版式）。
4. 表格元素自动分页（表头 / 表尾重复）验证。
✅ 里程碑：选中记录一键出 PDF / 打印，版式正确。

## 6. P4（可选）静默打印
1. 下载安装 PrintDot Client 桌面端（Windows 优先）。
2. 插件连接客户端，调用静默打印（无人值守直打）。
⚠️ 需用户机安装客户端；首期可不做。

## 7. P5（可选）团队共享（Supabase）
1. 建 Supabase 表存模板 JSON；对接 Repository 接口。
2. 支持跨表 / 跨团队模板共享与权限。
⚠️ 仅当有多团队复用需求才做。

## 8. 上线发布（稳定后二选一）
- 方案 X（继续自定义 URL）：`npm run build` → 把 dist 部署到免费静态托管（GitHub Pages / Vercel / Netlify，自带 HTTPS）→ 多维表格自定义插件地址改为该 HTTPS 地址。
- 方案 Y（opdev 上传发布，仍属「自定义插件」能力，非记录视图扩展）：装 `@lark-opdev/cli` → `opdev login` → 脚手架配 AppId / BlockTypeID（app.json / block.json）→ `npm run upload` → 开放平台发布。
✅ 里程碑：团队成员可在飞书多维表格直接使用插件。

## 9. 关键风险与约束
- AGPL-3.0：不改 vue-print-designer 源码即可免费商用；改源码且闭源需商业授权（联系作者 0ldFive）。
- 生产地址必须 HTTPS（免费托管满足，不必买域名）。
- 若插件接自有后端（如 Supabase），需配飞书「服务器域名白名单」。

## 10. 待你拍板的决策点
1. 模板存储：先多维表格字段，还是直接 Supabase？
2. 是否做静默打印（P4）？
3. 上线走「自定义 URL 免费托管」还是「opdev 上传」？
4. 首批支持哪些单据 / 纸张尺寸？
